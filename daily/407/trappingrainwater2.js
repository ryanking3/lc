"use strict";
function trapRainWater(heightMap) {
    //heightmap m x n int matrix representing height of each unit cell in 2d elev map, return vol of water can trap
    const m = heightMap.length;
    const n = heightMap[0].length;
    console.log(m + "" + n);
    return 2;
}
;
